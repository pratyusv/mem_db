{
  "name": "mem-db",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript mem_db server",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/index.js",
    "dev": "tsc -w -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.14.10"
  }
}
